openapi: 3.0.3
info:
  title: DeFarm Check API
  description: |
    API de verificação de compliance socioambiental para o agronegócio brasileiro.

    Agrega múltiplas fontes de dados públicos governamentais para validar conformidade
    de produtores, propriedades e produtos rurais.

    **Fontes de dados:**
    - Lista Suja do Trabalho Escravo (MTE) - 678 registros
    - Embargos Ambientais (IBAMA) - 65,953 documentos
    - Desmatamento Histórico (PRODES/INPE) - Dados geoespaciais anuais
    - Alertas de Desmatamento em Tempo Real (DETER/INPE) - Alertas diários últimos 90 dias
    - Terras Indígenas (FUNAI) - Todas TIs demarcadas no Brasil
    - Unidades de Conservação (ICMBio) - Todas UCs federais, estaduais e municipais
    - Cadastro Ambiental Rural (CAR/SICAR) - Cobertura nacional 27 estados

    **Documentação completa:** https://github.com/gabrielrondon/defarm-check-api
  version: 2.1.0
  contact:
    name: DeFarm Support
    email: suporte@defarm.com
    url: https://defarm.com
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: https://check.89.167.96.182.sslip.io
    description: Production server
  - url: http://localhost:3000
    description: Local development server

tags:
  - name: check
    description: Compliance check operations
  - name: sources
    description: Data source information
  - name: samples
    description: Sample data for testing
  - name: health
    description: Health check endpoints

security:
  - ApiKeyAuth: []

paths:
  /check:
    post:
      tags:
        - check
      summary: Execute compliance check
      description: |
        Executa verificação de compliance socioambiental para um input (CNPJ, CPF, CAR, coordenadas ou endereço).

        A API consulta múltiplas fontes em paralelo e retorna um veredito agregado com score de 0-100.

        **Tipos de Input:**
        - **CNPJ/CPF**: Documentos de pessoa física ou jurídica (com ou sem formatação)
        - **CAR**: Número do Cadastro Ambiental Rural (formato: UF-MUNICÍPIO-CÓDIGO)
        - **COORDINATES**: Coordenadas GPS (lat/lon em decimal WGS84)
        - **ADDRESS**: Endereço brasileiro em texto livre (geocodificado automaticamente)

        ---

        ## 🔍 Checkers Executados Automaticamente

        A API executa até **13 checkers** em paralelo, dependendo do tipo de input:

        ### Social & Legal (CNPJ/CPF)
        - **Slave Labor Registry**: Lista Suja do Trabalho Escravo (MTE)
        - **CGU Sanctions**: Sanções federais (CEIS, CNEP, CEAF)

        ### Environmental - Document-based (CNPJ/CPF)
        - **IBAMA Embargoes**: Embargos ambientais por documento

        ### Environmental - CAR-based
        - **CAR Registry**: Status do CAR (ativo/cancelado/suspenso)
        - **🔄 CAR x PRODES Intersection**: Cruza geometria do CAR com polígonos PRODES para detectar desmatamento DENTRO da propriedade ⭐
        - **MapBiomas Validated Deforestation**: Desmatamento validado por analistas dentro do CAR
        - **INPE Fire Hotspots**: Focos de calor/queimadas dentro do CAR (últimos 90 dias)
        - **ANA Water Use Permits**: Outorgas de uso de água próximas ao CAR

        ### Environmental - Spatial (COORDINATES/ADDRESS)
        - **PRODES Deforestation**: Verifica se coordenadas caem em polígono de desmatamento histórico
        - **DETER Real-Time Alerts**: Alertas de desmatamento em tempo real (últimos 90 dias)
        - **Indigenous Lands**: Verifica sobreposição com Terras Indígenas (FUNAI)
        - **Conservation Units**: Verifica sobreposição com UCs (ICMBio)
        - **IBAMA Embargoes (Spatial)**: Embargos em buffer de 5km das coordenadas
        - **MapBiomas Validated Deforestation**: Desmatamento validado próximo às coordenadas
        - **INPE Fire Hotspots**: Focos de calor próximos às coordenadas
        - **ANA Water Use Permits**: Outorgas próximas às coordenadas

        ### Positive/Certification (CNPJ)
        - **MAPA Organic Certification**: Certificação orgânica (quando disponível)

        ---

        ## 🔄 Intersecções Espaciais (Spatial Crosses)

        **O que são?** Alguns checkers fazem intersecções geométricas (PostGIS) para detectar problemas dentro de áreas:

        1. **CAR x PRODES Intersection** ⭐ (Mais importante)
           - Input: CAR number
           - Cruza: Geometria do CAR (propriedade) com polígonos PRODES (desmatamento)
           - Retorna: Área desmatada dentro da propriedade, anos, número de polígonos
           - Severity: CRITICAL se desmatamento recente (≤2 anos) ou área grande (≥100 ha)

        2. **CAR x MapBiomas Alerta**
           - Input: CAR number
           - Cruza: Geometria do CAR com alertas validados MapBiomas
           - Retorna: Alertas de desmatamento dentro da propriedade

        3. **CAR x Queimadas**
           - Input: CAR number
           - Cruza: Geometria do CAR com focos de calor (últimos 90 dias)
           - Retorna: Número de focos dentro da propriedade

        4. **CAR x ANA Outorgas**
           - Input: CAR number
           - Cruza: Geometria do CAR com pontos de outorga
           - Retorna: Outorgas dentro ou próximas à propriedade

        5. **COORDINATES x PRODES/TIs/UCs/etc**
           - Input: COORDINATES ou ADDRESS (geocoded)
           - Verifica: Se ponto cai dentro de polígonos de restrição

        **Tecnologia:** PostGIS `ST_Intersects()`, `ST_Contains()`, `ST_Within()`

        ---

        ## 🌍 Geocoding (ADDRESS)

        - Endereços são convertidos automaticamente para coordenadas
        - Provider: OpenStreetMap Nominatim (gratuito)
        - Suporta abreviações de estado (PA → Pará, SP → São Paulo, etc.)
        - Cache de 1 ano (requisições subsequentes: <100ms)
        - Rate limit: 1 req/s para geocoding (Nominatim policy)

        ---

        ## ⚡ Performance

        - Primeira requisição: ~200ms (CNPJ/CPF/CAR), ~1-2s (ADDRESS c/ geocoding)
        - Com cache: ~10ms (checkers), ~100ms (ADDRESS cached)
        - Cache TTL: 7 dias (checkers), 1 ano (geocoding)
        - **Checkers executam em paralelo** (não sequencial)

        **Rate limit:** 10,000 requisições/minuto por API key
      operationId: executeCheck
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CheckRequest'
            examples:
              cnpj:
                summary: Verificar CNPJ
                value:
                  input:
                    type: CNPJ
                    value: "12345678000190"
              cnpj_formatted:
                summary: CNPJ com formatação
                value:
                  input:
                    type: CNPJ
                    value: "12.345.678/0001-90"
              cpf:
                summary: Verificar CPF
                value:
                  input:
                    type: CPF
                    value: "12345678900"
              coordinates:
                summary: Verificar coordenadas (desmatamento)
                value:
                  input:
                    type: COORDINATES
                    value:
                      lat: -7.0945
                      lon: -61.089
              address:
                summary: Verificar por endereço (geocoding automático)
                value:
                  input:
                    type: ADDRESS
                    value: "Altamira, Pará"
              address_full:
                summary: Endereço completo
                value:
                  input:
                    type: ADDRESS
                    value: "São Paulo, SP"
              with_options:
                summary: Com opções customizadas
                value:
                  input:
                    type: CNPJ
                    value: "12345678000190"
                  options:
                    useCache: true
                    includeEvidence: true
                    timeout: 30000
      responses:
        '200':
          description: Check executado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckResponse'
              examples:
                compliant:
                  summary: Produtor conforme (100%)
                  value:
                    checkId: "a84b07fb-8142-4cc3-bcf4-a59e368be37c"
                    input:
                      type: "CNPJ"
                      value: "11222333000144"
                    timestamp: "2026-01-29T10:30:00.000Z"
                    verdict: "COMPLIANT"
                    score: 100
                    sources:
                      - name: "Slave Labor Registry"
                        category: "social"
                        status: "PASS"
                        message: "Not found in slave labor registry"
                        executionTimeMs: 15
                        cached: false
                      - name: "IBAMA Embargoes"
                        category: "environmental"
                        status: "PASS"
                        message: "No active IBAMA embargoes found"
                        executionTimeMs: 18
                        cached: false
                    summary:
                      totalCheckers: 2
                      passed: 2
                      failed: 0
                      warnings: 0
                      errors: 0
                      notApplicable: 0
                    metadata:
                      processingTimeMs: 185
                      cacheHitRate: 0
                      apiVersion: "1.0.0"
                      timestamp: "2026-01-29T10:30:00.000Z"
                non_compliant:
                  summary: Produtor não conforme (Lista Suja)
                  value:
                    checkId: "b1c2d3e4-f5g6-h7i8-j9k0-l1m2n3o4p5q6"
                    verdict: "NON_COMPLIANT"
                    score: 50
                    sources:
                      - name: "Slave Labor Registry"
                        category: "social"
                        status: "FAIL"
                        severity: "CRITICAL"
                        message: "Found in slave labor registry: 41.297.068 GILBERTO ELENO BATISTA DOS SANTOS"
                        details:
                          employerName: "41.297.068 GILBERTO ELENO BATISTA DOS SANTOS"
                          type: "CNPJ"
                          state: "SP"
                          address: "ROD. PREFEITO JOAQUIM SIMÃO, KM 735, CENTRO, IGARATÁ/SP"
                          year: 2024
                          workersAffected: 2
                          cnae: "4399-1/03"
                          inclusionDate: "06/10/2025"
                          recommendation: "CRITICAL: Immediate compliance review required."
                        evidence:
                          dataSource: "Ministério do Trabalho e Emprego - Cadastro de Empregadores"
                          url: "https://www.gov.br/trabalho-e-emprego/pt-br/assuntos/inspecao-do-trabalho/areas-de-atuacao/cadastro_empregadores.xlsx"
                          lastUpdate: "2026-01-28"
                        executionTimeMs: 22
                        cached: false
                    summary:
                      totalCheckers: 2
                      passed: 1
                      failed: 1
                      warnings: 0
                    metadata:
                      processingTimeMs: 210
                      cacheHitRate: 0
                deforestation:
                  summary: Área com desmatamento detectado
                  value:
                    verdict: "NON_COMPLIANT"
                    score: 0
                    sources:
                      - name: "PRODES Deforestation"
                        category: "environmental"
                        status: "FAIL"
                        severity: "HIGH"
                        message: "Deforestation detected: 15ha in 2024"
                        details:
                          area_ha: 15
                          year: 2024
                          municipality: "Novo Aripuanã"
                          state: "AM"
                          path_row: "231/066"
                          coordinates:
                            lat: -7.0945
                            lon: -61.089
                          recommendation: "HIGH: Deforestation detected at this location."
                        evidence:
                          dataSource: "INPE PRODES - Programa de Monitoramento do Desmatamento"
                          url: "http://terrabrasilis.dpi.inpe.br/"
                          lastUpdate: "2025-12-01"
                deter_alert:
                  summary: Alerta recente de desmatamento (DETER)
                  value:
                    verdict: "NON_COMPLIANT"
                    score: 0
                    sources:
                      - name: "DETER Real-Time Alerts"
                        category: "environmental"
                        status: "FAIL"
                        severity: "CRITICAL"
                        message: "Recent deforestation alert detected: DESMATAMENTO_VEG (3 days ago)"
                        details:
                          alertDate: "2026-01-26"
                          daysAgo: 3
                          areaHa: 42.5
                          municipality: "Lábrea"
                          state: "AM"
                          classname: "DESMATAMENTO_VEG"
                          sensor: "WFI"
                          pathRow: "231/066"
                          coordinates:
                            lat: -7.0945
                            lon: -61.089
                          recommendation: "CRITICAL: Recent deforestation detected. This location has been flagged for illegal deforestation in the last week."
                        evidence:
                          dataSource: "INPE DETER-B - Sistema de Detecção de Desmatamento em Tempo Real"
                          url: "http://terrabrasilis.dpi.inpe.br/"
                          lastUpdate: "2026-01-29"
                indigenous_land:
                  summary: Sobreposição com Terra Indígena
                  value:
                    verdict: "NON_COMPLIANT"
                    score: 0
                    sources:
                      - name: "Indigenous Lands"
                        category: "environmental"
                        status: "FAIL"
                        severity: "CRITICAL"
                        message: "Location overlaps with Indigenous Land: Yanomami"
                        details:
                          terraIndigena: "Yanomami"
                          etnia: "Yanomami"
                          phase: "Homologada"
                          phaseDescription: "officially recognized by Presidential decree"
                          areaHa: 9664975
                          municipality: "Alto Alegre"
                          state: "RR"
                          coordinates:
                            lat: 2.5
                            lon: -63.8
                          recommendation: "CRITICAL: This location is within Indigenous Land. Economic activities strictly prohibited by law."
                        evidence:
                          dataSource: "FUNAI - Fundação Nacional dos Povos Indígenas"
                          url: "https://www.gov.br/funai/pt-br/atuacao/terras-indigenas"
                          lastUpdate: "2026-01-29"
                conservation_unit:
                  summary: Sobreposição com Unidade de Conservação
                  value:
                    verdict: "NON_COMPLIANT"
                    score: 0
                    sources:
                      - name: "Conservation Units"
                        category: "environmental"
                        status: "FAIL"
                        severity: "CRITICAL"
                        message: "Location overlaps with Conservation Unit: Parque Nacional do Jaú"
                        details:
                          unidadeConservacao: "Parque Nacional do Jaú"
                          category: "Parque Nacional"
                          group: "Proteção Integral"
                          groupDescription: "Full Protection - economic activities strictly prohibited"
                          areaHa: 2272000
                          municipality: "Novo Airão"
                          state: "AM"
                          sphere: "Federal"
                          coordinates:
                            lat: -1.9
                            lon: -61.6
                          recommendation: "CRITICAL: Full Protection Conservation Unit. Economic activities strictly prohibited."
                        evidence:
                          dataSource: "ICMBio - Instituto Chico Mendes de Conservação da Biodiversidade"
                          url: "https://www.gov.br/icmbio/pt-br/assuntos/biodiversidade/unidade-de-conservacao"
                          lastUpdate: "2026-01-29"
                car_check:
                  summary: Propriedade com CAR cancelado
                  value:
                    verdict: "NON_COMPLIANT"
                    score: 25
                    sources:
                      - name: "CAR - Cadastro Ambiental Rural"
                        category: "environmental"
                        status: "FAIL"
                        severity: "HIGH"
                        message: "Location has cancelado CAR registration: MT-5102504-0123456789ABCDEF"
                        details:
                          carNumber: "MT-5102504-0123456789ABCDEF"
                          carStatus: "CANCELADO"
                          ownerDocument: "12345678000190"
                          ownerName: "Fazenda Exemplo Ltda"
                          propertyName: "Fazenda São João"
                          areaHa: 1250
                          municipality: "Lucas do Rio Verde"
                          state: "MT"
                          coordinates:
                            lat: -13.05
                            lon: -55.91
                          recommendation: "CRITICAL: CAR cancelled. Property is irregular according to Forest Code."
                        evidence:
                          dataSource: "SICAR - Sistema Nacional de Cadastro Ambiental Rural"
                          url: "https://www.car.gov.br/publico/imoveis/index?car=MT-5102504-0123456789ABCDEF"
                          lastUpdate: "2026-01-29"
        '400':
          description: Bad Request - Input inválido
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad Request"
                message: "Invalid input type. Must be one of: CNPJ, CPF, COORDINATES"
                statusCode: 400
        '401':
          description: Unauthorized - API key inválida ou ausente
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Unauthorized"
                message: "Missing API key. Include X-API-Key header."
                statusCode: 401
        '429':
          description: Too Many Requests - Rate limit excedido
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Too Many Requests"
                message: "Rate limit exceeded. Maximum 10,000 requests per minute."
                statusCode: 429
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Internal Server Error"
                message: "An unexpected error occurred"
                statusCode: 500

  /checks/{id}:
    get:
      tags:
        - check
      summary: Get check by ID
      description: |
        Busca resultado de uma verificação anterior pelo ID.

        Útil para:
        - Recuperar resultados históricos
        - Implementar polling de checks assíncronos
        - Auditar verificações passadas
      operationId: getCheckById
      parameters:
        - name: id
          in: path
          required: true
          description: ID único da verificação (UUID)
          schema:
            type: string
            format: uuid
          example: "a84b07fb-8142-4cc3-bcf4-a59e368be37c"
      responses:
        '200':
          description: Check encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CheckResponse'
        '404':
          description: Check não encontrado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Not Found"
                message: "Check not found"
                statusCode: 404

  /sources:
    get:
      tags:
        - sources
      summary: List all data sources and checkers
      description: |
        Lista todas as fontes de dados (checkers) disponíveis na API.

        **Total:** 13 checkers ativos (social, legal, environmental, certification)

        **Útil para:**
        - Descobrir quais checkers estão ativos
        - Verificar status operacional de cada fonte
        - Entender categorias disponíveis
        - Ver quais input types cada checker suporta

        **Nota:** Alguns checkers fazem intersecções espaciais (spatial crosses) usando PostGIS.
        Exemplo: "CAR x PRODES Intersection" cruza geometria do CAR com polígonos PRODES.
      operationId: listSources
      security:
        - ApiKeyAuth: []
      responses:
        '200':
          description: Lista de fontes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SourceInfo'
              example:
                - name: "Slave Labor Registry"
                  category: "social"
                  enabled: true
                  status: "operational"
                  description: "Verifica se CNPJ/CPF está na Lista Suja do Trabalho Escravo (MTE)"
                - name: "IBAMA Embargoes"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica embargos ambientais ativos do IBAMA"
                - name: "PRODES Deforestation"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Detecta desmatamento histórico anual em coordenadas (INPE PRODES)"
                - name: "DETER Real-Time Alerts"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Detecta alertas de desmatamento em tempo real (últimos 90 dias) - INPE DETER-B"
                - name: "Indigenous Lands"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica sobreposição com Terras Indígenas demarcadas (FUNAI)"
                - name: "Conservation Units"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica sobreposição com Unidades de Conservação (ICMBio)"
                - name: "CAR - Cadastro Ambiental Rural"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica regularização ambiental via CAR - Cobertura nacional 27 estados (SICAR)"
                - name: "CAR x PRODES Intersection"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Cruza geometria do CAR com polígonos PRODES para detectar desmatamento dentro da propriedade (intersecção espacial PostGIS)"
                - name: "MapBiomas Validated Deforestation"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Desmatamento validado por analistas (MapBiomas Alerta) - suporta CAR e coordenadas"
                - name: "INPE Fire Hotspots"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Focos de calor/queimadas detectados por satélites (últimos 90 dias) - suporta CAR e coordenadas"
                - name: "ANA Water Use Permits"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Outorgas de uso de recursos hídricos (ANA) - suporta CAR e coordenadas"

  /sources/{category}:
    get:
      tags:
        - sources
      summary: List sources by category
      description: |
        Lista fontes de dados filtradas por categoria.

        **Categorias disponíveis:**
        - `social`: Checkers de compliance social (trabalho escravo, etc)
        - `environmental`: Checkers ambientais (embargos, desmatamento, CAR)
        - `legal`: Checkers legais (em desenvolvimento)
      operationId: listSourcesByCategory
      security:
        - ApiKeyAuth: []
      parameters:
        - name: category
          in: path
          required: true
          description: Categoria das fontes
          schema:
            type: string
            enum:
              - environmental
              - social
              - legal
          example: "environmental"
      responses:
        '200':
          description: Lista de fontes da categoria
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SourceInfo'
              example:
                - name: "IBAMA Embargoes"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica embargos ambientais ativos do IBAMA"
                - name: "PRODES Deforestation"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Detecta desmatamento histórico anual em coordenadas (INPE PRODES)"
                - name: "DETER Real-Time Alerts"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Detecta alertas de desmatamento em tempo real (INPE DETER-B)"
                - name: "Indigenous Lands"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica sobreposição com Terras Indígenas (FUNAI)"
                - name: "Conservation Units"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica sobreposição com Unidades de Conservação (ICMBio)"
                - name: "CAR - Cadastro Ambiental Rural"
                  category: "environmental"
                  enabled: true
                  status: "operational"
                  description: "Verifica regularização ambiental via CAR (SICAR)"

  /health:
    get:
      tags:
        - health
      summary: Health check
      description: |
        Verifica status de saúde da API e serviços dependentes.

        **Status possíveis:**
        - `ok`: Todos os serviços operacionais
        - `degraded`: Alguns serviços offline mas API ainda funcional
        - `down`: API não operacional

        **Serviços monitorados:**
        - Database (PostgreSQL + PostGIS)
        - Cache (Redis)
      operationId: healthCheck
      security: []
      responses:
        '200':
          description: API operacional
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
              example:
                status: "ok"
                timestamp: "2026-01-29T10:30:00.000Z"
                version: "1.0.0"
                services:
                  database: "ok"
                  redis: "ok"
        '503':
          description: API indisponível ou degradada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
              example:
                status: "degraded"
                timestamp: "2026-01-29T10:30:00.000Z"
                version: "1.0.0"
                services:
                  database: "ok"
                  redis: "down"

  /samples/all:
    get:
      tags:
        - samples
      summary: Get one sample from each data source
      description: |
        Retorna um exemplo de cada fonte de dados disponível (overview completo).

        **Útil para:**
        - Overview rápido de todas as fontes
        - Testes end-to-end da API
        - Demos para clientes
        - Verificar quais fontes estão populadas

        **Fontes incluídas:**
        - `listaSuja`: CPF/CNPJ na Lista Suja do Trabalho Escravo
        - `ibama`: CPF/CNPJ com embargos IBAMA
        - `terrasIndigenas`: Coordenadas dentro de Terras Indígenas
        - `unidadesConservacao`: Coordenadas dentro de UCs (pode ser null se API offline)
        - `deter`: Alertas DETER recentes (pode ser null se API offline)
        - `car`: CAR com status irregular (CA/SU/PE)
        - `prodes`: Coordenadas com desmatamento PRODES

        **Nota:** Alguns campos podem ser `null` se as APIs governamentais estiverem
        temporariamente indisponíveis (não bloqueante - checkers tratam isso retornando PASS).

        Cada exemplo inclui um `testUrl` pronto para usar no endpoint `/check`.
      operationId: getSamplesAll
      security: []
      responses:
        '200':
          description: Samples retornados com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  listaSuja:
                    type: object
                    nullable: true
                  ibama:
                    type: object
                    nullable: true
                  terrasIndigenas:
                    type: object
                    nullable: true
                  unidadesConservacao:
                    type: object
                    nullable: true
                  deter:
                    type: object
                    nullable: true
                  car:
                    type: object
                    nullable: true
                  prodes:
                    type: object
                    nullable: true

  /samples/lista-suja:
    get:
      tags:
        - samples
      summary: Get samples from Lista Suja do Trabalho Escravo
      description: |
        Retorna 10 exemplos aleatórios de CPF/CNPJ presentes na Lista Suja do Trabalho Escravo (MTE).

        **Dados:** 664 registros de empregadores flagrados com trabalho escravo ou análogo.

        **Input types suportados:** CPF, CNPJ

        **Uso:**
        Use os documentos retornados (`document` field) para testar o endpoint `/check`
        e verificar que a API realmente detecta trabalho escravo (status: FAIL, severity: CRITICAL).

        **Response:** Cada sample inclui documento, nome, estado, ano da infração e trabalhadores afetados.
      operationId: getSamplesListaSuja
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object
                properties:
                  source:
                    type: string
                  count:
                    type: integer
                  samples:
                    type: array
                    items:
                      type: object
                      properties:
                        document:
                          type: string
                        documentFormatted:
                          type: string
                        name:
                          type: string
                        type:
                          type: string
                        state:
                          type: string
                        year:
                          type: integer
                        workersAffected:
                          type: integer
                        testUrl:
                          type: string

  /samples/ibama:
    get:
      tags:
        - samples
      summary: Get samples from IBAMA Environmental Embargoes
      description: |
        Retorna 10 exemplos aleatórios de CPF/CNPJ com embargos ambientais do IBAMA.

        **Dados:** 122,814 documentos agregados com embargos ambientais.

        **Input types suportados:** CPF, CNPJ, COORDINATES (buffer 5km)

        **Uso:**
        Use os documentos retornados para testar detecção de embargos IBAMA.
        API retorna FAIL quando embargo_count > 0.

        **Response:** Cada sample inclui documento, nome, número de embargos e área total embargada (hectares).
      operationId: getSamplesIbama
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object
                properties:
                  source:
                    type: string
                  count:
                    type: integer
                  samples:
                    type: array
                    items:
                      type: object

  /samples/terras-indigenas:
    get:
      tags:
        - samples
      summary: Get samples from Indigenous Lands (Terras Indígenas)
      description: |
        Retorna 10 coordenadas aleatórias dentro de Terras Indígenas demarcadas (FUNAI).

        **Dados:** 649 territórios indígenas com geometrias PostGIS.

        **Input types suportados:** COORDINATES, ADDRESS (via geocoding)

        **Uso:**
        Use as coordenadas retornadas (centróide de cada TI) para testar o endpoint `/check`.
        API retorna FAIL (severity: CRITICAL) quando coordenadas caem dentro de Terra Indígena.

        **Response:** Cada sample inclui nome da TI, etnia, fase de regularização, área (ha),
        município, estado e coordenadas do centróide.
      operationId: getSamplesTerrasIndigenas
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object
                properties:
                  source:
                    type: string
                  count:
                    type: integer
                  samples:
                    type: array
                    items:
                      type: object

  /samples/unidades-conservacao:
    get:
      tags:
        - samples
      summary: Get samples from Conservation Units (Unidades de Conservação)
      description: |
        Retorna até 10 coordenadas aleatórias dentro de Unidades de Conservação (ICMBio).

        **Dados:** Federais, estaduais e municipais (quando disponível).

        **Input types suportados:** COORDINATES, ADDRESS (via geocoding)

        **Uso:**
        Use as coordenadas retornadas (centróide de cada UC) para testar o endpoint `/check`.
        API retorna FAIL quando coordenadas caem dentro de UC de proteção integral.

        **⚠️ Nota importante:**
        Se `count: 0`, significa que a API ICMBio está temporariamente indisponível.
        Neste caso, o endpoint retorna um `message` explicativo. Isso é esperado e
        não bloqueante - o checker retorna PASS quando não há dados disponíveis.

        **Response:** Cada sample inclui nome da UC, categoria, grupo, área (ha),
        município, estado, esfera administrativa e coordenadas do centróide.
      operationId: getSamplesUnidadesConservacao
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object

  /samples/deter:
    get:
      tags:
        - samples
      summary: Get samples from DETER Real-Time Alerts
      description: |
        Retorna até 10 coordenadas aleatórias com alertas DETER recentes (últimos 90 dias).

        **Dados:** Alertas de desmatamento em tempo real do INPE/TerraBrasilis.

        **Input types suportados:** COORDINATES, ADDRESS (via geocoding)

        **Uso:**
        Use as coordenadas retornadas (centróide de cada alerta) para testar o endpoint `/check`.
        API retorna FAIL quando coordenadas caem próximas a alertas DETER recentes.

        **⚠️ Nota importante:**
        Se `count: 0`, significa que a API INPE/TerraBrasilis está temporariamente indisponível.
        Neste caso, o endpoint retorna um `message` explicativo. Isso é esperado e
        não bloqueante - o checker retorna PASS quando não há dados disponíveis.

        **Response:** Cada sample inclui data do alerta, área (ha), município, estado,
        classificação, sensor e coordenadas do centróide.
      operationId: getSamplesDeter
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object

  /samples/car:
    get:
      tags:
        - samples
      summary: Get CAR registrations with irregular status
      description: |
        Retorna até 10 registros CAR (Cadastro Ambiental Rural) com status irregular.

        **Dados:** 8,096,127 registros CAR de 27 estados brasileiros (SICAR).

        **Input types suportados:** CAR (código completo), COORDINATES, ADDRESS (via geocoding)

        **Status Codes SICAR:**
        - `CA` (CANCELADO): Registro cancelado
        - `SU` (SUSPENSO): Registro suspenso por irregularidades
        - `PE` (PENDENTE): Registro pendente de validação

        **Uso:**
        Use o `carNumber` retornado para testar o endpoint `/check` com input type CAR.
        Também retorna coordenadas do centróide para testes espaciais.
        API retorna FAIL quando CAR está cancelado ou suspenso.

        **⚠️ Fix aplicado (2026-02-02):**
        Query SQL corrigido para usar códigos SICAR (CA/SU/PE) ao invés de nomes completos.
        Adicionado campo `statusDescription` para mapear códigos → nomes legíveis.

        **Response:** Cada sample inclui carNumber, status (código), statusDescription (legível),
        área (ha), município, estado e coordenadas do centróide.

        Se `count: 0`, significa que não há CARs com status irregular no momento
        (ou dados em processamento). Neste caso retorna `message` explicativo.
      operationId: getSamplesCar
      security: []
      responses:
        '200':
          description: Lista de CAR samples com status irregular (ou mensagem explicativa se vazio)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/SampleResponse'
                  - type: object
                    properties:
                      samples:
                        type: array
                        items:
                          $ref: '#/components/schemas/CARSample'

  /samples/prodes:
    get:
      tags:
        - samples
      summary: Get samples from PRODES Deforestation
      description: |
        Retorna 10 coordenadas aleatórias com desmatamento histórico detectado pelo PRODES.

        **Dados:** 216,252 polígonos de desmatamento de todos os 6 biomas brasileiros (INPE).

        **Input types suportados:** COORDINATES, ADDRESS (via geocoding)

        **Biomas cobertos:**
        - Amazônia Legal (2015-2024)
        - Cerrado, Caatinga, Mata Atlântica, Pampa, Pantanal

        **Uso:**
        Use as coordenadas retornadas (centróide de cada polígono) para testar o endpoint `/check`.
        API retorna FAIL quando coordenadas caem dentro de polígono de desmatamento.
        Severity baseada na área e ano do desmatamento.

        **Response:** Cada sample inclui ano do desmatamento, área (ha), município, estado,
        path/row (referência satélite) e coordenadas do centróide.
      operationId: getSamplesProdes
      security: []
      responses:
        '200':
          description: 10 samples retornados
          content:
            application/json:
              schema:
                type: object

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: |
        API key para autenticação. Formato: `ck_` + 64 caracteres hexadecimais.

        Entre em contato com o time DeFarm para obter uma API key.

        **Rate limit:** 10,000 requisições/minuto por key.

  schemas:
    CheckRequest:
      type: object
      required:
        - input
      properties:
        input:
          type: object
          required:
            - type
            - value
          properties:
            type:
              type: string
              enum:
                - CNPJ
                - CPF
                - CAR
                - COORDINATES
                - ADDRESS
              description: Tipo do input a ser verificado
            value:
              oneOf:
                - type: string
                  description: |
                    CNPJ, CPF, CAR (com ou sem formatação) ou ADDRESS (endereço brasileiro em texto livre).

                    Exemplos de endereço:
                    - "São Paulo, SP"
                    - "Altamira, Pará"
                    - "Brasília, DF"
                    - "Rua Augusta 123, São Paulo"

                    Endereços são geocodificados automaticamente para coordenadas usando OpenStreetMap Nominatim.
                - type: object
                  description: Coordenadas geográficas
                  required:
                    - lat
                    - lon
                  properties:
                    lat:
                      type: number
                      format: float
                      minimum: -90
                      maximum: 90
                      description: Latitude em decimal (WGS84)
                    lon:
                      type: number
                      format: float
                      minimum: -180
                      maximum: 180
                      description: Longitude em decimal (WGS84)
        options:
          type: object
          description: Opções adicionais para a verificação
          properties:
            sources:
              type: array
              items:
                type: string
              description: Lista de checkers específicos a executar (opcional)
            useCache:
              type: boolean
              default: true
              description: Usar cache para acelerar resposta
            includeEvidence:
              type: boolean
              default: true
              description: Incluir evidências e links para fontes
            timeout:
              type: integer
              default: 15000
              description: Timeout em milissegundos para cada checker

    CheckResponse:
      type: object
      required:
        - checkId
        - input
        - timestamp
        - verdict
        - score
        - sources
        - summary
        - metadata
      properties:
        checkId:
          type: string
          format: uuid
          description: ID único da verificação
        input:
          type: object
          properties:
            type:
              type: string
            value:
              oneOf:
                - type: string
                - type: object
          description: Input original da requisição
        timestamp:
          type: string
          format: date-time
          description: Data/hora da verificação (ISO 8601)
        verdict:
          type: string
          enum:
            - COMPLIANT
            - NON_COMPLIANT
            - PARTIAL
            - UNKNOWN
          description: |
            Veredito final agregado:
            - `COMPLIANT`: Totalmente conforme (score = 100)
            - `NON_COMPLIANT`: Não conforme (score < 80)
            - `PARTIAL`: Parcialmente conforme (80 ≤ score < 100)
            - `UNKNOWN`: Erro ao executar verificação
        score:
          type: integer
          minimum: 0
          maximum: 100
          description: |
            Score de compliance (0-100):
            - 100 = Totalmente conforme
            - 80-99 = Parcialmente conforme
            - 0-79 = Não conforme
        sources:
          type: array
          items:
            $ref: '#/components/schemas/SourceResult'
          description: Resultado de cada checker executado
        summary:
          type: object
          required:
            - totalCheckers
            - passed
            - failed
            - warnings
            - errors
          properties:
            totalCheckers:
              type: integer
              description: Total de checkers executados
            passed:
              type: integer
              description: Checkers que passaram (PASS)
            failed:
              type: integer
              description: Checkers que falharam (FAIL)
            warnings:
              type: integer
              description: Checkers com alertas (WARNING)
            errors:
              type: integer
              description: Checkers com erro de execução
            notApplicable:
              type: integer
              description: Checkers não aplicáveis ao input
        metadata:
          type: object
          required:
            - processingTimeMs
            - cacheHitRate
            - apiVersion
            - timestamp
          properties:
            processingTimeMs:
              type: integer
              description: Tempo total de processamento em ms
            cacheHitRate:
              type: number
              format: float
              minimum: 0
              maximum: 1
              description: Taxa de cache hits (0-1)
            apiVersion:
              type: string
              description: Versão da API
            timestamp:
              type: string
              format: date-time

    SourceResult:
      type: object
      required:
        - name
        - category
        - status
        - message
      properties:
        name:
          type: string
          description: Nome da fonte/checker
        category:
          type: string
          enum:
            - social
            - environmental
            - legal
          description: Categoria do checker
        status:
          type: string
          enum:
            - PASS
            - FAIL
            - WARNING
            - ERROR
            - NOT_APPLICABLE
          description: |
            Status do check:
            - `PASS`: Conforme (nenhum problema encontrado)
            - `FAIL`: Não conforme (problema detectado)
            - `WARNING`: Alerta (atenção necessária)
            - `ERROR`: Erro ao executar checker
            - `NOT_APPLICABLE`: Checker não aplicável ao input
        severity:
          type: string
          enum:
            - LOW
            - MEDIUM
            - HIGH
            - CRITICAL
          description: |
            Severidade do problema (apenas quando status = FAIL):
            - `LOW`: Problema menor
            - `MEDIUM`: Problema moderado
            - `HIGH`: Problema grave
            - `CRITICAL`: Problema crítico (bloqueante)
        message:
          type: string
          description: Mensagem descritiva do resultado
        details:
          type: object
          description: Detalhes específicos do resultado (varia por checker)
        evidence:
          type: object
          properties:
            dataSource:
              type: string
              description: Nome da fonte de dados oficial
            url:
              type: string
              format: uri
              description: URL da fonte de dados
            lastUpdate:
              type: string
              format: date
              description: Data da última atualização dos dados
            raw:
              type: object
              description: Dados brutos da fonte (opcional)
        executionTimeMs:
          type: integer
          description: Tempo de execução do checker em ms
        cached:
          type: boolean
          description: Se o resultado veio do cache

    SourceInfo:
      type: object
      required:
        - name
        - category
        - enabled
        - status
        - description
      properties:
        name:
          type: string
          description: Nome da fonte de dados
        category:
          type: string
          enum:
            - social
            - environmental
            - legal
          description: Categoria da fonte
        enabled:
          type: boolean
          description: Se a fonte está habilitada
        status:
          type: string
          enum:
            - operational
            - down
            - maintenance
          description: Status operacional da fonte
        description:
          type: string
          description: Descrição da fonte e o que ela verifica

    HealthResponse:
      type: object
      required:
        - status
        - timestamp
        - version
        - services
      properties:
        status:
          type: string
          enum:
            - ok
            - degraded
            - down
          description: Status geral da API
        timestamp:
          type: string
          format: date-time
          description: Data/hora da verificação
        version:
          type: string
          description: Versão da API
        services:
          type: object
          required:
            - database
            - redis
          properties:
            database:
              type: string
              enum:
                - ok
                - down
              description: Status do PostgreSQL + PostGIS
            redis:
              type: string
              enum:
                - ok
                - down
              description: Status do Redis (cache)

    SampleResponse:
      type: object
      description: Response padrão para endpoints /samples/*
      required:
        - source
        - count
        - samples
      properties:
        source:
          type: string
          description: Nome da fonte de dados
          example: "CAR - Cadastro Ambiental Rural (SICAR)"
        count:
          type: integer
          description: Número de samples retornados (0-10)
          minimum: 0
          maximum: 10
          example: 10
        message:
          type: string
          nullable: true
          description: |
            Mensagem explicativa quando count=0.
            Indica que APIs governamentais estão offline (não bloqueante).
          example: "Dados em processamento. API temporariamente indisponível."
        samples:
          type: array
          description: Lista de samples (vazio se count=0)
          items:
            type: object

    CARSample:
      type: object
      description: Sample de registro CAR com status irregular
      required:
        - carNumber
        - status
        - statusDescription
        - areaHa
        - municipality
        - state
        - coordinates
        - testUrl
      properties:
        carNumber:
          type: string
          description: Código único do registro CAR (formato UF-MUNICÍPIO-CÓDIGO)
          example: "PR-4111506-1B80A47993684BEE9908ED4468199BF8"
        status:
          type: string
          enum: [CA, SU, PE, AT]
          description: Código de status SICAR
          example: "CA"
        statusDescription:
          type: string
          enum: [CANCELADO, SUSPENSO, PENDENTE, ATIVO]
          description: Descrição legível do status
          example: "CANCELADO"
        areaHa:
          type: integer
          description: Área do imóvel em hectares
          example: 150
        municipality:
          type: string
          description: Município do imóvel
          example: "Ivaipora"
        state:
          type: string
          description: Sigla do estado (UF)
          example: "PR"
        coordinates:
          type: object
          description: Coordenadas do centróide da propriedade
          required:
            - lat
            - lon
          properties:
            lat:
              type: number
              format: float
              description: Latitude (WGS84)
              example: -24.33476771879231
            lon:
              type: number
              format: float
              description: Longitude (WGS84)
              example: -51.587721288619306
        testUrl:
          type: string
          description: URL de exemplo para testar no endpoint /check
          example: 'POST /check {"input":{"type":"CAR","value":"PR-4111506-..."}}'

    Error:
      type: object
      required:
        - error
        - message
        - statusCode
      properties:
        error:
          type: string
          description: Nome do erro
        message:
          type: string
          description: Mensagem descritiva do erro
        statusCode:
          type: integer
          description: Código HTTP do erro
        details:
          type: array
          items:
            type: object
          description: Detalhes adicionais (validação, etc)
